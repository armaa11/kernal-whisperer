# kernal-whisperer
Kernel Whisperer is a deterministic-first Linux debugging CLI. It traces syscalls, correlates runtime failures, filters noise, and outputs actionable root-cause analysis with concrete fixes. Optional local LLM support improves explanation quality, while deterministic evidence remains the source of truth.
